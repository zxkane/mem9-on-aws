{
  "registryId": "123456789012",
  "scanningConfiguration": {
    "scanType": "BASIC",
    "rules": [
      {
        "scanFrequency": "SCAN_ON_PUSH",
        "repositoryFilters": [
          {
            "filter": "mem9-on-aws/mnemo-server",
            "filterType": "WILDCARD"
          },
          {
            "filter": "mem9-on-aws/qwen3-embed",
            "filterType": "WILDCARD"
          }
        ]
      }
    ]
  }
}
