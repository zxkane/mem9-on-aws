{
  "registryId": "123456789012",
  "scanningConfiguration": {
    "scanType": "BASIC",
    "rules": [
      {
        "scanFrequency": "SCAN_ON_PUSH",
        "repositoryFilters": [
          {
            "filter": "sibling-project/*",
            "filterType": "WILDCARD"
          }
        ]
      }
    ]
  }
}
