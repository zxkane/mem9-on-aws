{
  "name": "mem9-on-aws-infra",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.21.0",
  "scripts": {
    "deploy": "cd .. && sst deploy",
    "remove": "cd .. && sst remove",
    "dev": "cd .. && sst dev",
    "pretest": "cd .. && infra/node_modules/.bin/sst install --stage test",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-agentcore-control": "^3.900.0",
    "@aws-sdk/client-ssm": "^3.700.0"
  },
  "devDependencies": {
    "@pulumi/pulumi": "3.215.0",
    "@types/node": "^24.0.0",
    "sst": "^4.17.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "yaml": "^2.9.0"
  }
}
